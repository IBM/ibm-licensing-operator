scorecard:
  output: text
  plugins:
    - basic:
        cr-manifest:
          - "./deploy/crds/operator.ibm.com_v1alpha1_ibmlicensing_cr.yaml"
        namespace: ibm-common-services
        init-timeout: 600
