global:
  imagePullPrefix: icr.io
  imagePullSecret: ibm-entitlement-key
  licenseAccept: true

ibmLicensing:
  namespace: ibm-licensing
  watchNamespace: ibm-licensing
  # Comma-separated list of namespaces to exclude from license collection.
  # Used only when operatorDeployment=false; the operator reads this from the CR spec otherwise.
  excludeNamespace: ""
  imageRegistryNamespaceOperator: cpopen
  imageRegistryNamespaceOperand: cpopen/cpfs
  # When false, deploys the License Service application directly without the operator.
  # The operator Deployment and IBMLicensing CR are suppressed; instead the operand
  # Deployment, Service, Secrets, and CRDs are rendered directly.
  operatorDeployment: true
  createRBAC: true
  createRBACInWatchedNamespaces: true
  spec:
    version: 4.2.25
    datasource: datacollector
    httpsEnable: true
    # httpsCertsSource and enableInstanaMetricCollection are used only when
    # operatorDeployment=false; the operator reads them from the CR spec otherwise.
    httpsCertsSource: external
    enableInstanaMetricCollection: false
    features:
      nssEnabled: false
      nodeCpuCappingEnabled: true
      kubeRBACAuthEnabled: true
      operandRequestsEnabled: true
      customResourcesEnabled: true
    # resources are used only when operatorDeployment=false for the operand pod.
    resources:
      limits:
        cpu: 500m
        memory: 1Gi
      requests:
        cpu: 200m
        memory: 256Mi
        ephemeralStorage: 256Mi
